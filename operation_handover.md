# PingEZ 運用引継ぎ資料

管理者向けの簡易引継ぎ資料です。  
本ツールは**確認専用**です。ネットワーク設定の変更は一切行いません。

---

## ツールの概要

`PingEZ.ps1` は、Windows端末上で動作するPowerShell製のネットワーク疎通確認ツールです。  
コマンドプロンプトを使わずに、GUI画面から以下の確認作業ができます。

- 複数機器へのping
- TCPポートの疎通確認
- tracert（ルートトレース）
- DNS正引き・逆引き

---

## どのような場面で使うか

### 1. 新規機器設置後の疎通確認

機器設置後、管理端末からpingやTCP確認で基本疎通を確認する場面。

```
宛先: 新規機器のIPアドレス
操作: Ping 確認 → TCP ポート確認
```

### 2. 障害発生時の現状確認

通信障害が発生した場合、どこまで疎通できるかを確認する場面。

```
宛先: デフォルトゲートウェイ / L3スイッチ / 上位機器 / 対象サーバ
操作: Ping 確認 → Tracert 確認
→ 結果を保存してベンダーや上位部署に送付
```

### 3. ポート開放確認

FW設定変更後にポートが開放されたか確認する場面。

```
宛先: 対象サーバのIPアドレス
ポート: 確認したいポート番号
操作: TCP ポート確認
```

### 4. DNS設定変更後の確認

DNS設定変更後に名前解決が正常に行われるか確認する場面。

```
宛先: 確認したいホスト名またはIPアドレス
操作: DNS 確認
```

### 5. 定期的な疎通確認と記録

重要サーバへの疎通状況を定期確認し、結果を記録として保管する場面。

```
宛先: 重要サーバ群のIPアドレスをまとめて入力
操作: Ping 確認 → 結果を保存
→ 日次・週次の確認記録として保管
```

---

## 宛先リストの活用

頻繁に確認する宛先グループをファイルに保存しておくことで、毎回の入力を省略できます。

### 保存手順

1. 宛先欄に確認対象の IP アドレス・ホスト名を入力
2. 「**リスト保存**」ボタンをクリック
3. 保存先とファイル名を指定（例: `targets_server.txt`）

### 読込手順

1. 「**リスト読込**」ボタンをクリック
2. 保存済みの TXT ファイルを選択
3. 宛先欄に自動入力される

### ファイル形式と管理例

```
# ゲートウェイ・L3スイッチ
192.168.1.1
192.168.1.2

# 業務サーバ
192.168.10.10
192.168.10.20
server01.example.local
```

- `#` で始まる行はコメントとして無視されます
- グループ・用途別にファイルを分けて管理することを推奨します

### 推奨管理構成

| ファイル名 | 用途 |
|------------|------|
| `targets_gateway.txt` | デフォルトゲートウェイ・L3スイッチ |
| `targets_server.txt` | 業務サーバ群 |
| `targets_infra.txt` | DNS・認証サーバなどインフラ系 |

---

## 結果保存の使い方

### 保存手順

1. 確認作業を実施（Ping / TCP / Tracert / DNS）
2. 「**結果を保存 (TXT/CSV)**」ボタンをクリック
3. 保存先フォルダと形式（TXT または CSV）を選択
4. 保存完了のメッセージを確認

### ファイル名の規則

保存ダイアログに自動入力されるファイル名の例:

```
nwcheck_20260526_153000.txt
```

日時を含むため、複数回保存しても上書きされません。  
必要に応じてファイル名を変更してください。

### 保存先の推奨

| 目的 | 推奨保存先 |
|------|------|
| 障害記録 | 障害チケットフォルダ / 共有フォルダ |
| 定期確認記録 | 月次フォルダなどの所定の場所 |
| ベンダー提出用 | デスクトップなど一時的な場所 |

---

## ベンダー問い合わせ時の活用例

### 送付内容の例

ベンダーや上位部署への問い合わせ時に、保存した結果ファイルを添付することで  
口頭説明を省き、正確な情報を伝えられます。

```
件名: 〇〇サーバへの疎通不可について

添付ファイル: nwcheck_20260526_153000.txt

【確認内容】
- 確認実施端末: 管理PC (192.168.1.100)
- 確認日時: 2026-05-26 15:30:00
- 使用ツール: PingEZ (社内疎通確認ツール)

【確認結果】
添付ファイルをご参照ください。
ping, TCP ポート確認, tracert の結果を含みます。
```

### 含めると有用な情報

- 確認元端末の IP アドレス
- 確認先の IP アドレス / ホスト名
- 確認日時
- tracert の結果（どのホップで止まっているか）
- TCP ポート確認の結果（オープン / クローズ）

---

## やってはいけないこと

### ツールの使い方に関して

| 禁止事項 | 理由 |
|------|------|
| 宛先欄にコマンド文字（`;` `|` `&` 等）を入力する | セキュリティ上の観点からスキップされるが、意図しない動作の原因になる |
| 結果ファイルを無断で外部送信する | 機密情報（内部IPアドレス、ホスト名等）が含まれる可能性がある |
| tracert を多数宛先に対して同時実行する | 処理に時間がかかり、端末の応答が遅くなる場合がある |
| ポート確認を短時間に大量に繰り返す | FW ログに記録される場合があり、誤検知のアラームが上がる可能性がある |

### ツールの機能に関して

| 禁止事項 | 備考 |
|------|------|
| 本ツールにネットワーク設定変更機能を追加する | 確認専用ツールとしての位置づけを維持すること |
| PowerShell 実行ポリシーを恒久的に `Unrestricted` に変更する | セキュリティポリシーに従った最小限の設定変更にとどめること |
| 本ツールをインターネット接続環境から実行する | 閉域環境向けのツールであり、外部通信の懸念がある場合は担当者に確認すること |

---

## トラブルシューティング

### ツールが起動しない

**原因候補**: PowerShell 実行ポリシーの制限  
**対処**: 以下のコマンドで一時的にポリシーを回避して実行

```powershell
powershell.exe -ExecutionPolicy Bypass -File .\PingEZ.ps1
```

---

### ping がすべて失敗する

**原因候補**: ICMP がファイアウォールで遮断されている  
**対処**: TCP ポート確認や DNS 確認で代替的に疎通を確認する

---

### DNS 確認が失敗する

**原因候補**: 端末の DNS サーバ設定が正しくない、または DNS サーバ自体に障害がある  
**対処**: `ipconfig /all` で DNS サーバ設定を確認し、DNS サーバへの ping を試みる

---

### tracert が応答しない・時間がかかる

**原因候補**: 途中のルータが ICMP を返さない設定になっている（`*` 表示）  
**対処**: 一定時間待つ（最大 60 秒でタイムアウトし自動終了します）

---

### 結果の保存に失敗する

**原因候補**: 保存先のフォルダに書き込み権限がない  
**対処**: デスクトップやドキュメントフォルダなど書き込み可能な場所を選択する

---

## 担当者連絡先

本資料の管理担当者・問い合わせ先を記載してください。

| 項目 | 内容 |
|------|------|
| 担当部署 | 情報政策課 ネットワーク管理担当 |
| 作成日 | 2026-05-26 |
| ツールバージョン | 0.1.0 (実機検証版) |
